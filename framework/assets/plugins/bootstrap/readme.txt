About Bootstrap build:
======================

1) Base CSS > Icons has been excluded for FontAwesome.

2) The following portion has been commented out because it causes problems if WP users are changing image sizes with TinyMCE editor:

img { width: auto\9; }